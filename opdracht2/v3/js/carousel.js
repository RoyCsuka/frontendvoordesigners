/*jslint devel: true */

var aantalPlaatjes = document.querySelectorAll('section img').length;
var plaatje = document.querySelector('section img').offsetWidth;
var widthCarousel = plaatje * aantalPlaatjes;

var section = document.querySelector(' main > section:first-of-type div');
var knopLinks = document.querySelector('section .links');
var knopRechts = document.querySelector('section .rechts');

function (plaatje) {
	if (plaatje = 0) {
		console.log('doe niks');
	} else {
		console.log('doe niks');
	}
}
